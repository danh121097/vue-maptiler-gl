/dist/*
.local
.output.js
/node_modules/**

**/*.svg
**/*.sh

/public/*

# Written by `bun run changelog` from git-cliff output, whose wrapping Prettier
# would rewrite; formatting it here would fail format:check on the next push.
# The ported file described a version history this package never had and was
# removed; this one starts at v2.0.2, the first release written from this
# repository's own commits.
docs/changelog.md
